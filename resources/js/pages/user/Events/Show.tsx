import { Inertia } from '@/wayfinder/types';
import { FC } from 'react';

const Show: FC<Inertia.Pages.User.Events.Show> = ({ event }) => {
    return <div>This is the event show page</div>;
};

export default Show;
