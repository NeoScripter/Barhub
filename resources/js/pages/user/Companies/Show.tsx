import { NodeProps } from '@/types/shared';
import { FC } from 'react';

const Show: FC<NodeProps> = ({ className }) => {
    return <div>Hello, this is companies show page</div>;
};

export default Show;
