includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    paths:
        - app
        - config
        - database
        - routes
        - tests

    # Level 0-9 (0 = least strict, 9 = most strict)
    level: 5

    # Ignore errors you want to exclude
    ignoreErrors:
        # - '#Unsafe usage of new static#'
